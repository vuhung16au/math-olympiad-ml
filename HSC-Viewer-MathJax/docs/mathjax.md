# MathJax In This Project

MathJax is loaded on the client and re-typesets reader content after route and content updates.

This project uses MathJax to improve online math rendering, while `make4ht` handles the initial LaTeX-to-HTML conversion.

The main hook lives in [lib/mathjax.ts](../lib/mathjax.ts).
