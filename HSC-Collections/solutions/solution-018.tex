\subsection{Solution to Problem 18: Curve on a Sphere}

\begin{solution}
The curve lies on the sphere $x^2 + y^2 + z^2 = 9$ of radius $3$. The given functions suggest using $z$ as the main parameter.

Let $t = z/3$, so $t \in [-1,1]$ as $z$ runs from $-3$ to $3$. The curve spirals three times around the $z$-axis, so the azimuthal angle should increase by $6\pi$ as $t$ goes from $-1$ to $1$. Take
\[
\theta = 3\pi t.
\]
At fixed height $z = 3t$, a point on the sphere has horizontal radius $\sqrt{9-z^2} = 3\sqrt{1-t^2}$.

Using the given functions $g(x)=\sqrt{9-x^2}$ and $f(x)=\cos(\pi x)$ with $x = z/3 = t$, a parameterisation is
\[
x(t) = 3\sqrt{1-t^2}\cos(3\pi t), \qquad
y(t) = 3\sqrt{1-t^2}\sin(3\pi t), \qquad
z(t) = 3t,
\qquad t \in [-1,1].
\]
Check that the curve stays on the sphere:
\begin{align*}
x^2 + y^2 + z^2
&= 9(1-t^2)\bigl(\cos^2(3\pi t)+\sin^2(3\pi t)\bigr) + 9t^2 \\
&= 9(1-t^2) + 9t^2 = 9.
\end{align*}
\end{solution}

\begin{takeaways}
\begin{itemize}
    \item Sphere constraint: $x^2+y^2+z^2=9$ with radius $3$
    \item Parameter $t=z/3$ makes the height vary linearly from $-3$ to $3$
    \item Spiralling: use $\theta=3\pi t$ and horizontal radius $3\sqrt{1-t^2}$
    \item The problem asks for "a possible set", not a unique solution
    \item The problem statement is not rigorous when it says "spirals 3 times" as this could be interpreted in different ways, relies entirely on the student's visual intuition
\end{itemize}
\end{takeaways}

\begin{remark}[Revived problem statement]
The following is a fixed version keeps the same geometric setup but makes the motion along the curve more explicit.

\medskip
\noindent\textbf{Problem 18 (revived): Curve on a Sphere}

A curve $C$ spirals 3 times around the sphere centred at the origin and with radius 3, as shown below.

\vspace{0.5em}
\noindent
\begin{center}
\includegraphics[width=0.6\textwidth]{images/18-01.png}
\end{center}
\vspace{0.3em}
% \noindent\textit{Figure 1: Curve $C$ spiraling around the sphere}

\vspace{0.5em}
A particle is initially at the point $(0, 0, -3)$ and moves along the curve $C$ on the surface of the sphere
\textbf{with constant vertical velocity},
ending at the point $(0, 0, 3)$.

By using the diagram below, which shows the graphs of the functions

$f_1(t)=\cos(\pi t)$, $f_2(t)=\sin(\pi t)$ and $g(t)=\sqrt{9-t^{2}}$,

and considering the graphs $f_1(t)g(t)$ and $f_2(t)g(t)$.



Give a possible set of parametric equations that describe the curve $C$.

\vspace{0.5em}
\noindent
\begin{center}
\begin{tikzpicture}[scale=1.2, >=stealth]
    % Grid lines for reference (very thin)
    \draw[gray!15, very thin, step=1.0] (-4.5, -1.2) grid (4.5, 3.7);

    % Draw axes
    \draw[->, >=stealth, thick] (-4.5, 0) -- (4.5, 0) node[below right] {$x$};
    \draw[->, >=stealth, thick] (0, -1.2) -- (0, 3.7) node[above left] {$y$};

    % Draw tick marks on x-axis
    \foreach \x in {-4,-3,-2,-1,1,2,3,4} {
        \draw (\x, 0.1) -- (\x, -0.1) node[below, font=\small] {$\x$};
    }
    \node[below left, font=\small] at (0,0) {$O$};

    % Draw tick marks on y-axis
    \foreach \y in {-1,1,2,3} {
        \draw (0.1, \y) -- (-0.1, \y) node[left, font=\small] {$\y$};
    }

    % Plot g(t) = sqrt(9-t^2) as a red semicircle
    \draw[very thick, bookred] (3,0) arc (0:180:3);
    \node[bookred, above left] at (-1.6, 2.4) {$g(t) = \sqrt{9-t^2}$};

    % Plot f_1(t) = cos(pi t) as a purple cosine curve
    \draw[very thick, bookpurple, samples=200, domain=-4:4] plot (\x, {cos(\x * 180)});
    \node[bookpurple, above right] at (3.1, 0.6) {$f_1(t) = \cos(\pi t)$};
\end{tikzpicture}
\end{center}
\vspace{0.3em}
% \noindent\textit{Figure 2: Graphs of $f_1(t)=\cos(\pi t)$ and $g(t)=\sqrt{9-t^{2}}$}

\vspace{0.5em}
\noindent
% \textbf{Note:} The problem statement is not mathematically rigorous; the phrase ``spirals 3 times'' relies entirely on the student's visual intuition to assume that the angular velocity of the particle around the $z$-axis is strictly constant with respect to its vertical displacement.

% \medskip
% Under the revived wording, a parameterisation matching the diagram is
% \[
% x(t) = \sqrt{9-t^2}\cos(\pi t), \qquad
% y(t) = \sqrt{9-t^2}\sin(\pi t), \qquad
% z(t) = t,
% \qquad t \in [-3,3].
% \]
% This is equivalent to the solution above: if the revived parameter is $s \in [-3,3]$, then with $t = s/3 \in [-1,1]$ we have $g(s) = 3\sqrt{1-t^2}$ and $\cos(\pi s) = \cos(3\pi t)$.
\end{remark}
