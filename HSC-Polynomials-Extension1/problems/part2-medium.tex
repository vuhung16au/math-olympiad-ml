% ============================================================
% Part 2 — Stretch Problems
% Multi-step practice problems:
%   polynomial long division, finding unknowns using Vieta's,
%   graphs with multiple roots, HSC-style exam questions
% ============================================================

\begin{problem}[A Family Determined by Four Intersections]
Interpret the intended question as follows: the horizontal line $y=k$ meets the quartic
\[
y=ax^4+bx^3+cx^2+dx+k
\]
at $x=-1,1,2,3$. Find $b$, $c$ and $d$ in terms of $a$.
\end{problem}

\begin{hintbox}
The intersections with $y=k$ are the roots of $ax^4+bx^3+cx^2+dx$.
\end{hintbox}

\begin{solution}
Since the curve meets $y=k$ at $x=-1,1,2,3$, the polynomial
\[
ax^4+bx^3+cx^2+dx
\]
has roots $-1,1,2,3$. So
\[
ax^4+bx^3+cx^2+dx=a(x+1)(x-1)(x-2)(x-3).
\]
Now
\[
(x+1)(x-1)=x^2-1,\qquad (x-2)(x-3)=x^2-5x+6,
\]
so
\[
(x^2-1)(x^2-5x+6)=x^4-5x^3+5x^2+5x-6.
\]
Therefore
\[
ax^4+bx^3+cx^2+dx=a(x^4-5x^3+5x^2+5x-6).
\]
Comparing coefficients,
\[
\boxed{b=-5a,\qquad c=5a,\qquad d=5a.}
\]
\end{solution}

\begin{takeaways}
\begin{itemize}[leftmargin=*]
\item Intersections with a horizontal line come from the shifted polynomial $P(x)-k$.
\item Once the roots are known, the factorised form is immediate up to scale.
\end{itemize}
\end{takeaways}

\begin{problem}[A Surd Expression from Quadratic Roots]
Let $x_1$ and $x_2$ be the distinct roots of
\[
x^2-7x+3=0.
\]
Calculate the exact value of
\[
A=\sqrt{11x_1+1}+\sqrt{13x_2+6}.
\]
\end{problem}

\begin{hintbox}
For each root $x$, use $x^2-7x+3=0$ to rewrite the expression under each square root as a perfect square.
\end{hintbox}

\begin{solution}
Since $x_1,x_2$ are roots of
\[
x^2-7x+3=0,
\]
Vieta gives
\[
x_1+x_2=7,\qquad x_1x_2=3.
\]
So both roots are positive.

Now use $x_i^2-7x_i+3=0$.

For $x_1$:
\[
11x_1+1=(11x_1+1)+(x_1^2-7x_1+3)=x_1^2+4x_1+4=(x_1+2)^2.
\]

For $x_2$:
\[
13x_2+6=(13x_2+6)+(x_2^2-7x_2+3)=x_2^2+6x_2+9=(x_2+3)^2.
\]

Hence
\[
A=\sqrt{(x_1+2)^2}+\sqrt{(x_2+3)^2}.
\]
Because $x_1,x_2>0$, we have $x_1+2>0$ and $x_2+3>0$, so
\[
A=(x_1+2)+(x_2+3)=x_1+x_2+5=7+5=\boxed{12}.
\]

Therefore:
\[
\text{exact value }=12,\qquad \text{approximate value }=12.0.
\]
\end{solution}

\begin{takeaways}
\begin{itemize}[leftmargin=*]
\item Adding the root equation $x^2-7x+3=0$ can turn linear expressions into perfect squares.
\item Vieta then collapses the final expression to a simple constant.
\end{itemize}
\end{takeaways}
